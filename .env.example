# Neon PostgreSQL connection string (read-only, table-scoped credential)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Anthropic API key for Claude Messages API
ANTHROPIC_API_KEY=sk-ant-...
